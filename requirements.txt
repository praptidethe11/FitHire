# FitHire — pinned dependencies for reproducible Stage 3 sandbox builds.
# CPU-only stack (no CUDA) — matches the "no GPU during ranking" constraint
# in submission_spec.md Section 3.

# --- Web app (backend/main.py) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20   # required by FastAPI's File(...)/Form(...) uploads

# --- Data handling ---
pandas==2.2.3

# --- Ranking / retrieval ---
rank-bm25==0.2.2

# --- Semantic scoring (bi-encoder + cross-encoder) ---
sentence-transformers==3.3.1
torch==2.5.1                # CPU wheel; see Dockerfile for the --index-url pin
huggingface-hub==0.27.0

# --- File parsing (JD upload: docx / pdf) ---
python-docx==1.1.2
PyPDF2==3.0.1
pypdf==5.1.0